import { StyleSheet } from "react-native";

export const Button = StyleSheet.create({
  box: {
    alignItems: "center",
    height: 55,
    justifyContent: "center",
    borderRadius: 3,
    margin:10,
  },
  label: {
      fontSize:22,
  },
});
