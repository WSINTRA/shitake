import { StyleSheet } from "react-native";

export const Button = StyleSheet.create({
  box: {
    alignItems: "center",
    padding: 10,
    height: 55,
    justifyContent: "center",
  },
  label: {
      fontSize:22,
  },
});
